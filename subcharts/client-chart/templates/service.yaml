apiVersion: v1
kind: Service
metadata:
  name: client-chart
  lables:
    {{- include "client-chart.labels" . | nindent 4 }}
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    {{- include "client-chart.selectorLabels" . | nindent 4 }}
    
      
 